#events-list
  for day in days
    .day
      .day-header
        p= day.date.calendar()
      .day-events
        table
          for event in day.events
            tr(class=event.get('id'))
              td.time
                span= moment.unix(event.get('date')).format('H:mm')
              td.info
                h2.name= event.get('name')
                p.location= event.get('location')

